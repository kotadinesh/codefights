bool checkPalindrome(std::string inputString) {
    std::string temp;
    temp=inputString;
    std::reverse(temp.begin(),temp.end());
    if(inputString==temp)
        return true;
    else 
        return false;
}
